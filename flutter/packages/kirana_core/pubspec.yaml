name: kirana_core
description: Shared models, API client, theme, and formatters for Kirana Saarthi.
version: 1.0.0
publish_to: none

environment:
  sdk: ^3.12.1
  flutter: '>=3.44.0'

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
